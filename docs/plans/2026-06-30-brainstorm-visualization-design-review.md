### Adversarial Review Report
**Phase:** design
**Artifact:** docs/plans/2026-06-30-brainstorm-visualization-design.md
**Status:** FAIL

**Findings (Critical):**
- None.

**Findings (Important):**
- `D1` [Repo-precedent conflicts / Multi-component validation] [`docs/plans/2026-06-30-brainstorm-visualization-design.md:42-45`, `:67-75`]: The validation strategy is grep-only for a behavior-changing skill edit. Repo precedent says skill edits follow TDD for process docs: "NO SKILL WITHOUT A FAILING TEST FIRST" and "This applies to NEW skills AND EDITS to existing skills" (`skills/writing-skills/SKILL.md:461-480`), with application/retrieval scenarios for technique/reference skills (`:482-529`). The design only asserts strings like `Visual Companion`, `just-in-time`, and `Mermaid`, which does not prove agents offer the companion at the right time, avoid offering it upfront, choose text vs visual per question, or load/use the guide. Recommendation: add a RED/GREEN skill-behavior scenario or pressure test for brainstorming before implementation, then prove post-change behavior with a representative transcript/eval; keep grep checks as regression guards, not primary proof.
- `D2` [Declared integration proof / User-intent drift / Existence-runtime-validity] [`docs/plans/2026-06-30-brainstorm-visualization-design.md:24-30`, `:78-82`]: The design replaces upstream’s browser-based visual companion with host-native Markdown/Mermaid guidance but does not define a host integration matrix or prove the real consumer surface. Upstream’s current `skills/brainstorming/SKILL.md` describes a browser companion that opens a tab, starts a server, and records browser events; upstream README also documents visual companion telemetry. ADK’s `docs/cross-llm-coverage.md:7-20` has no visual/Mermaid rendering capability row, and `tests/cross-llm-coverage.md:13` only tracks brainstorming’s question-tool host conditionals. Recommendation: explicitly state the parity boundary as an accepted non-goal, add a host-by-host matrix marking Markdown/Mermaid/browser behavior as `runtime-integrated`, `config-only`, or `deferred`, and require at least one real-host rendering proof or mark rendering as best-effort text-only fallback.
- `D3` [Missing failure modes / Assumptions under attack] [`docs/plans/2026-06-30-brainstorm-visualization-design.md:39-45`, `:80-82`, `:90-91`]: The design covers “agent overuses diagrams” and plain-text fallback, but misses likely first failures: invalid Mermaid syntax, hosts that display Mermaid as raw code, diagrams that contradict the text design, stale visual artifacts after the conversation moves on, and inaccessible visuals where the fallback is lower-fidelity than the decision being made. Recommendation: add explicit failure rules: text remains the source of truth, every visual must include a concise text equivalent, agents must fall back when rendering is unavailable or invalid, and visual artifacts must be updated or retired when decisions change.

**Findings (Minor):**
- `D4` [Artifact-class precedent / YAGNI violations / Simpler alternative not considered] [`docs/plans/2026-06-30-brainstorm-visualization-design.md:39-41`, `:78-83`]: The design adds `skills/brainstorming/visual-companion.md` without justifying that it is heavy reference material. Skill-authoring precedent says supporting files are “Only if needed” (`skills/writing-skills/SKILL.md:74-82`), keep principles/concepts and code patterns under 50 lines inline (`:86-93`), and use heavy reference files when material is too large (`:451-459`). Recommendation: either make the initial change a short inline `## Visual Companion` section, or bound the guide’s role/size and explain why it is a heavy reference rather than SKILL.md content.
- `D5` [Repo-precedent conflicts] [`docs/plans/2026-06-30-brainstorm-visualization-design.md:36-40`]: The design says the guide will include Mermaid examples while existing skill process diagrams use `dot`, and `skills/writing-skills/SKILL.md:377-405` gives Graphviz/dot flowchart guidance plus render tooling for skill diagrams. If Mermaid is only for user-facing generated artifacts, that distinction is not explicit. Recommendation: state that SKILL.md process flow remains `dot`; Mermaid examples are only optional output artifacts shown to users where supported.
- `D6` [Infrastructure impact / Artifact-class precedent] [`docs/plans/2026-06-30-brainstorm-visualization-design.md:42-45`, `tests/pipeline-evidence-doc-sync.sh:1-4`, `:68-91`]: `tests/pipeline-evidence-doc-sync.sh` is currently framed as a regression guard for pipeline evidence issues #69/#70/#71/#72 and later #79, not a general feature-behavior test bucket. Adding #78 visual assertions there may blur the script’s contract and make future maintenance less obvious. Recommendation: either update the script header/sectioning to include #78 explicitly or create a focused skill-behavior/content regression test for brainstorming visualization.

**Bug-class scan transcript:**
| Class | Result | Note |
|---|---|---|
| Project-guidance conflicts | Clean | No `docs/design-guidance.md` exists per prompt; the design cites repo canon from `AGENTS.md`, `README.md`, and `skills/brainstorming/SKILL.md`, which is an acceptable substitute here. |
| Assumptions under attack | Finding | `D2` and `D3`: the assumptions that “workflow guidance” is enough and Mermaid/text fallback is acceptable are load-bearing and insufficiently proven. |
| Repo-precedent conflicts | Finding | `D1` conflicts with `skills/writing-skills/SKILL.md` TDD-for-skill-edits precedent; `D5` risks conflicting with dot/Graphviz skill-diagram precedent. |
| Artifact-class precedent | Finding | `D4` questions the unproven need for a sibling guide file; `D6` questions using `pipeline-evidence-doc-sync.sh` for this feature’s regression assertions. |
| YAGNI violations | Finding | `D4`: a separate companion guide may be more structure than needed for the first host-neutral version. |
| Missing failure modes | Finding | `D3`: invalid Mermaid, non-rendering hosts, stale/contradictory visuals, and accessibility degradation are not designed through. |
| Security/privacy architecture | Clean | Current scoped design avoids browser server, network service, dependencies, and says visual examples must not expose secrets/PII; future browser parity would need a new privacy decision, especially because upstream documents telemetry. |
| Infrastructure impact | Finding | `D6`: no cloud/runtime infra is added, but the test-suite impact is under-specified for a script with an existing narrower contract. |
| Multi-component validation | Finding | `D1` and `D2`: grep checks do not prove the skill behavior or host rendering boundary. |
| Declared integration proof | Finding | `D2`: host-native Markdown/Mermaid rendering is treated as available/acceptable without a host integration matrix or real-host proof. |
| Contributed UI rendering proof | Clean | The design does not register a UI route/widget into a host shell; this class is not directly applicable unless browser companion/runtime work is reintroduced. |
| Rollback story | Clean | `docs/plans/2026-06-30-brainstorm-visualization-design.md:94-97` gives a concrete markdown/test revert path with no migrations or runtime state. |
| Simpler alternative not considered | Finding | `D4`: the design considers copying upstream and building a runtime, but not a minimal inline-only section that avoids a new sidecar guide. |
| User-intent drift | Finding | `D2`: the user pointed at upstream’s visualization companion; the design may underdeliver by shipping chat/Markdown guidance unless the reduced parity boundary is explicitly accepted. |
| Existence/runtime-validity | Finding | `D2`: the design does not verify the emitted visual artifacts target a real rendering/consumer surface across ADK-supported hosts. |

**Options the author may not have considered:**
1. **Inline-only MVP:** Add a concise `## Visual Companion` section directly to `skills/brainstorming/SKILL.md` with just-in-time offer rules, per-question visual/text decision rules, and plain-text fallback. This is smaller, follows the “keep inline” precedent, and can be behavior-tested before introducing a heavier guide.
2. **Two-tier parity design:** Ship host-neutral Markdown/Mermaid guidance now, but explicitly add a deferred “browser companion parity” track with acceptance criteria copied from upstream: browser launch, session security, event capture, telemetry policy, cleanup, and multi-host process behavior. This prevents the current PR from being mistaken for full upstream parity.
3. **Capability-matrix-first approach:** Before changing brainstorming behavior, update `docs/cross-llm-coverage.md` and `tests/cross-llm-coverage.md` with visual-rendering capabilities per host. Then make the skill branch on that matrix: rendered Mermaid where proven, Markdown/text fallback elsewhere.

**Verdict reasoning:** The design is directionally reasonable but not ready to pass adversarial review because its validation does not prove the behavioral skill change, and its scope reduction from upstream’s browser companion to Markdown/Mermaid guidance is not backed by a real host capability matrix or explicit accepted parity boundary. These are Important findings because they can cause the implementation to pass grep tests while failing the actual user-visible goal: agents reliably offering and using useful brainstorm/design visuals across ADK-supported hosts.
